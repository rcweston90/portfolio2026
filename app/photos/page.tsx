import PhotosClient from './PhotosClient';

export const metadata = {
  title: 'Photos',
  description: 'A collection of photographs displayed in a collage layout',
};

export default function PhotosPage() {
  return <PhotosClient />;
}

